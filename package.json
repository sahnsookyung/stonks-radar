{
  "name": "financial-research-workbench",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/web",
    "packages/shared-types"
  ],
  "scripts": {
    "web:dev": "npm --workspace apps/web run dev",
    "web:build": "npm --workspace apps/web run build",
    "web:test": "npm --workspace apps/web run test",
    "web:lint": "npm --workspace apps/web run lint",
    "web:e2e": "npm --workspace apps/web exec playwright test",
    "build:map-assets": "node scripts/build_map_assets.mjs",
    "seed:snapshots": "python3 scripts/build_seed_snapshots.py",
    "check:schemas": "python3 scripts/validate_snapshots.py",
    "api:compile": "python3 -m compileall apps/api/src apps/worker/src apps/fetch-sandbox/src scripts",
    "api:test": "uv run --project apps/api --extra dev --with ./apps/fetch-sandbox pytest -q tests",
    "deploy:preflight": "python3 scripts/deploy_preflight.py",
    "ops:storage": "python3 scripts/monitor_storage.py",
    "test": "npm run check:schemas && npm run web:test && npm run api:compile",
    "test:all": "npm run check:schemas && npm run web:test && npm run api:test && npm run web:e2e && npm run api:compile",
    "build": "npm run build:map-assets && npm run seed:snapshots && npm run web:build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  }
}
