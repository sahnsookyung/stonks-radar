[project]
name = "frw-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["frw-api"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
